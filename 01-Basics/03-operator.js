/*
1.assignment(=,+=,-=,*=,%=,/=)
2.arithmatic(+,-,%,*,/,**)
3.comparision(>,<,==,<=,>=,/=)
4.logical(&&,||,!)
5.bitwise(&,|,~,^,<<,>>,>>>)
6.typeof(typeof,instanceof)
7.string(concatination)
    Ex-let fname="Subhashree";
        let lname="Smita";
        let name=fname+lname;
 */
        let fname="Subhashree";
        let lname="Smita";
        let name=fname+lname;
        console.log(name);

/*
==(used to check same or not(return true)(check only value))
===(used to check value with datatype)
*/
    let a=5;
    let b='5';
    console.log(a==b);

    let a1=5;
    let b1="5";
    console.log(a1===b1);
